<%= simple_form_for(@verification) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :integer %>
    <%= f.input :assigned_to %>
    <%= f.input :business_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
