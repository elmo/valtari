<%= simple_form_for([:private, @deal_room ,@deal_room_upload ||= DealRoomUpload.new ], multipart: true ) do |f| %>
  <%= f.error_notification %>
  <%= f.file_field :upload, multiple: true  %>
  <%= f.button :submit, 'Save' %>
<% end %>
