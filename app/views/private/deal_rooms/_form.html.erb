<%= simple_form_for([:private, @deal_room]) do |f| %>

  <div class="form-inputs">
    <%= f.input :name -%>
    <%= f.input :company_name -%>
  </div>

  <div class="form-actions">
    <%= f.button :submit  %>
  </div>

<% end %>
