<div id="note-<%=note.id-%>" class="note">
  <div class='note-meta'><%= note.created_at.strftime(date_and_time_format) -%>
    <%= link_to my_business_note_path(notable, note), method: :delete  do %>
      <i class="material-icons">delete</i>
    <% end %>
    </div>
  <%= best_in_place note, :body, url:  my_business_note_path(notable, note) -%>
</div> <!-- note-->

