<div class="row">
  <div class="col s8 m8">
    <h1>Events</h1>
      <%= month_calendar events: @events do |date, events| %>
      <%= date %>
      <% events.each do |event| %>
        <div>
          <%= link_to event.title,  my_event_path(event)  %>
         </div>
       <% end %>
      <% end %>
    </div>
  </div>
  <p><%= link_to 'Create a new event', new_my_event_path %></p>
</div>
