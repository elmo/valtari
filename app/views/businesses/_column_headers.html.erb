<tr>
  <th id="name" class="<%= sort_arrow_class('company-name') -%>"><%= link_to 'Company', url_for(params.merge(sort: 'company-name', sort_direction: @sort_direction) ) -%></th>
  <th>Industry</th>
  <th>Verification</th>
  <th id="contact" class="<%= sort_arrow_class('owner-ceo') -%>" ><%= link_to 'Owner/CEO', url_for(params.merge(sort: 'owner-ceo', sort_direction: @sort_direction) ) -%></th>
  <th id="revenue" class="<%= sort_arrow_class('revenue' )-%>" ><%= link_to 'Revenue', url_for(params.merge(sort: 'revenue', sort_direction: @sort_direction) ) -%></th>
  <th id="employees" class="<%= sort_arrow_class('employees' ) -%>" ><%= link_to 'Employees', url_for(params.merge(sort: 'employees', sort_direction: @sort_direction) ) -%></th>
  <th id="city" class="<%= sort_arrow_class('city' ) -%>" ><%= link_to 'City', url_for(params.merge(sort: 'city', sort_direction: @sort_direction) ) -%></th>
  <th id="state" class="<%= sort_arrow_class('state' ) -%>" ><%= link_to 'State', url_for(params.merge(sort: 'state', sort_direction: @sort_direction) ) -%></th>
</tr>
