<h1>Revenues</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @revenues.each do |revenue| %>
      <tr>
        <td><%= revenue.name %></td>
        <td><%= link_to 'Edit', edit_admin_revenue_path(revenue) %></td>
        <td><%= link_to 'Destroy', admin_revenue_path(revenue), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Revenue', new_admin_revenue_path %>
